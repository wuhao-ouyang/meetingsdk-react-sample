apiVersion: v1
kind: Service
metadata:
  name: {{ include "meetingsdk-react-sample.fullname" . }}
  labels:
    {{- include "meetingsdk-react-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meetingsdk-react-sample.selectorLabels" . | nindent 4 }}
