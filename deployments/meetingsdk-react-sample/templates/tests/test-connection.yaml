apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meetingsdk-react-sample.fullname" . }}-test-connection"
  labels:
    {{- include "meetingsdk-react-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meetingsdk-react-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
