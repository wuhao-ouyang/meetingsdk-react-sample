apiVersion: v1
kind: Service
metadata:
  name: {{ include "caterpie.fullname" . }}
  labels:
    {{- include "caterpie.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caterpie.selectorLabels" . | nindent 4 }}
