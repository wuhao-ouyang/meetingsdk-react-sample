apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caterpie.fullname" . }}-test-connection"
  labels:
    {{- include "caterpie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caterpie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
